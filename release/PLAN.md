# Launch preparation acceptance plan

This pass preserves computational runtime d15b0bc and evaluation 741e528. It does not reopen optimization. The separate clean Git repository imports only the curated product tree, with file-level provenance in BASELINE_EXPORT.json; the research repository and frozen packages stay private and unchanged.

Dependency order and finite gates:

1. Verify product/evidence and current HM main; preserve existing branches, uncommitted work and frozen artifacts. **Done.**
2. Audit Rust consumer boundaries; provide idiomatic source, compile, carve and bounded cleave examples; build the packaged crate as an external dependency. **Done; final audit receipt accompanies the source archive.**
3. Curate source, logos, licenses, notices, documentation and public benchmark tables. Scan every publishable Git object/ref and nested artifact, then establish a source-only distribution decision if binary licensing remains unresolved. **Done; final audit receipt accompanies the source archive.**
4. Build uniquely versioned private alpha1 packages. Validate CLI/Python/Node/Rust outside the checkout, optional-backend compatibility and native/source/numerical tests. Preserve alpha0 bytes. **Done; exact final snapshot/artifact identities are recorded in the private launch handoff.**
5. Independently implement the real HM demographics source adapter in staging, preserving source/view/center-mask/ordered-sum/rounding. Validate legacy, shadow, eligible Skarve, failures and rollback. Hosting/R2 gates remain explicit; no production activation. **Done locally in separate HM branch; live staging/R2 qualification remains an explicit gate.**
6. Independently implement and visually validate HM /skarve with unchanged logos, scoped native charts, tested examples and accurate publication/adoption status. **Done locally in separate HM branch; live staging/R2 qualification remains an explicit gate.**
7. Freeze audited source/artifact hashes, private checkpoints, exact owner approval scopes and one launch recommendation. **Done; exact final snapshot/artifact identities are recorded in the private launch handoff.**

Resource policy: ordinary correctness work may run in parallel; performance measurements require a serial lane with no competing builds. Reuse existing dependencies and read-only data. New launch-generated build/install/report machinery is bounded to 4 GiB total, max two Rust compile workers, and no cloud traffic until its precise allowance is granted. This is separate from, and does not change, the completed benchmark's recorded limits. No unique historical data or work is deleted to make room. No new service, architecture, format, numerical policy or speculative performance change.
